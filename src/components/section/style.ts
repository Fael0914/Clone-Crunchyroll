import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  imagem: {
    width: 180,
    height: 250,
    marginRight: 10,
    borderRadius: 10,
  },

  tituloCard: {
    color: '#fff',
    marginTop: 10,
    fontSize: 15,
  }
});
