import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

  container: {
    flex: 1,
    backgroundColor: 'black',

  },

  carouselContainer: {
    marginBottom: 50,
    marginLeft: 10

  },

  tituloContainer: {
    color: '#fff',
    fontSize: 20,
    marginBottom: 15,
    fontWeight: 'bold',
    marginLeft: 10
  },
});